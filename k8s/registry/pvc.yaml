apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: registry-local-path
  resources:
    requests:
      storage: 10Gi
