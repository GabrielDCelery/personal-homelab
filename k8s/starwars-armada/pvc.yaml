apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: starwars-armada-build
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: starwars-armada-build-local-path
  resources:
    requests:
      storage: 5Gi
